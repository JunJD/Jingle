# @jingle/langchain-agent-harness

LangChain / LangGraph-backed runtime package for Jingle agent execution.

This package is intentionally allowed to depend on LangChain. Agent client and React packages should not know LangChain message classes, checkpoint channel values, or interrupt payloads.

Current runtime owner:

- run steering buffer and harness-owned LangChain middleware injection; the host passes a per-run steering buffer, not a middleware factory
- prompt builders, skill-source ordering, run/resume config, checkpoint lookup config, resume Command builder, submitted message and invoke initial-state builders, ToolRuntime run/tool-call id readers, tool-result ToolMessage/update Command builders, AIMessage tool-call mapper, and LangGraph interrupt guard
- Runtime module assembly through `createRuntime({ modules })`, with `RuntimeThread` as the control surface for invoke/resume/compact/run completion behavior
- `RuntimeGraph` owns the operation-driven LangGraph execution topology; legacy middleware-shaped nodes remain internal engine details, not public API
- abort-aware run stream draining through `drainRuntimeRunStream`, with host ports still owning HITL persistence and IPC fan-out while the runtime owns LangChain runtime/run trace config binding and trace callback assembly through a host trace port
- per-thread checkpointer manager lifecycle, with saver creation and close flushers injected by the host
- shared/root agent-loop middleware topology
- filesystem, summarization, and skills middleware construction, with backend, model, prompts, and skill sources injected by RuntimeModule contributions; compaction uses a Jingle-owned handoff-oriented context compaction prompt
- Jingle skill source ordering, with canonical workspace lookup at `.jingle/skills`
- generic guardrail middleware
- LangChain message text/tool-call-signal/HumanMessage refs and cloning readers used by host middleware adapters, plus trace callback lifecycle, skipped run-name handling, input summary hashing/preview, usage extraction, and error capture for observability callbacks
- title middleware, title state schema, and title policy helpers for generated-title parsing, first-exchange eligibility, tool-call pending suppression, and title prompt construction from LangChain messages; the host injects model-provider selection, timeout, and logging through a title generator port, not a title middleware instance
- Jingle-owned LangGraph state contract for `title`, `contextInclusions`, and `artifacts` schemas/reducers; artifact state stores manifests and presentation receipts only, while durable artifact content stays in the host artifact service
- web search LangChain tool shell for the `web_search` name, JSON schema, description, query validation, and middleware registration, with search execution injected by the host
- desktop automation LangChain tool shell for tool names, JSON schemas, descriptions, and middleware registration, with native parser/runner handlers injected by the host
- generic human-approval middleware for LangGraph interrupt/resume and approval gating
- LangGraph HITL interrupt reader for checkpoint/values state, values interrupt request-id projection, and values-stream HITL persistence orchestration, with product review parsing and persistence ports injected by the host
- raw LangGraph stream reader for message chunks, stream message display context / metadata hints, tool-call/tool-call-chunk normalization, streaming tool-call chunk accumulation, values snapshots, values/messages current-turn assistant selector, host stream-frame projection, values host projection with canonical HITL request-id overlay, and values state shape validation
- checkpoint-derived run status mapping for HITL interrupts
- raw LangGraph checkpoint reader for messages, todos, context inclusion channel, title channel, checkpoint-derived thread facts, serialized message facts, and serialized-message metadata hints
- checkpoint-to-host-message-row projection helper, with assistant display context and metadata hints normalized before host display/metadata adapters run
- checkpoint todo/context-inclusion/title projection helpers for LangGraph channel defaults and title normalization
- checkpoint message channel version reader and ancestry traversal helper for message-limited thread cloning, with message-state lookup injected by the host
- LangGraph checkpoint storage-shape helpers for messages / `__pregel_tasks` channels, canonical `__jingleRef` pending-write message refs, checkpoint manifest copying, and string channel-version validation
- checkpoint after-commit handler, with event and HITL persistence ports injected by the host
- run completion helpers, with checkpoint/no-checkpoint persistence, memory inclusion recording, failed/aborted-run finalization, and run lifecycle event ports injected by the host

Jingle-specific services, policy, message display/metadata, and Prisma schema ownership stay outside this package. For example, runtime code injects thread/run-scoped product options/ports and passes the LocalSandbox backend, model, skill sources, execute-command description, filesystem prompt, title generator port, per-run steering buffer, toolApproval policy port, and trace config/event ports into the harness; artifact/extension/context middleware still own their product state updates while delegating LangGraph tool-result message/update Command construction to harness, extension AI middleware owns display/presentation binding while delegating LangChain `AIMessage.tool_calls` cloning to harness, title model-provider selection and generation timeout/logging stay in main through a harness title generator port, workspace file context middleware owns workspace file selection and file reads while consuming harness HumanMessage refs/cloning helpers, trace metadata construction and DB event writing stay in main through the harness `trace.createRuntimeConfig` / `trace.createRunConfig` / `trace.recordEvent` port while runtime/run binding, callback assembly and skipped run-name handling belong to harness, applies Jingle execute-command metadata through a port, keeps Jingle `ToolApprovalItem` and permission policy in main through the `toolApproval` host port, maps LangGraph stream facts to Jingle `Message` / runtime event shapes in `src/main/agent/agent-stream-codec.ts` and `src/main/agent/agent-thread-runner.ts` while consuming harness stream readers, tool-call chunk accumulator, values/messages current-turn assistant selector, and values host projection for raw `contextInclusions`/`todos` shape, keeps stream IPC cloning in `src/main/agent/service.ts`, injects assistant display content and composer metadata adapters into checkpoint message projection from `src/main/agent/runtime-state.ts` after harness normalizes checkpoint display context and metadata hints, maps checkpoint-derived thread facts through a single `extractThreadFactsFromCheckpoint` main adapter while keeping Jingle `Todo` enum/context-inclusion product type casts in main, maps serialized message reads and metadata hints to Prisma message-state rows in `src/main/db/message-state.ts`, maps checkpoint lookup config to Jingle checkpointer calls in `src/main/agent/persistence.ts` and `src/main/threads/service.ts`, maps checkpoint storage-shape helpers to Prisma checkpoint rows/blobs/writes in `src/main/checkpointer/prisma-saver.ts`, maps checkpoint after-commit and values-stream HITL persistence ports to Jingle event/HITL tables in `src/main/checkpointer/runtime-checkpointer.ts` and `src/main/agent/service.ts`, maps checkpoint-derived status to Jingle run/thread rows in `src/main/agent/persistence.ts`, maps checkpoint ancestry results to DB clone operations in `src/main/threads/service.ts`, maps run completion/abort/fail ports to Jingle persistence/event/memory functions in `src/main/agent/service.ts`, and injects `RuntimeCheckpointSaver` plus projection flushers into the checkpointer manager instead of making this package import `@shared/*` or `src/main/*`.

Delegated-work spawning is intentionally not part of the current low-level runtime. Future multi-session and parent-child session work should enter through the Jingle work item / session coordination model first, then inject the minimal execution ports this package needs. The old task-tool projection, middleware insertion point, and read-only guardrail have been removed until that product model is explicit.

Desktop automation keeps the same boundary: the harness owns the LangChain `tool()` declarations and middleware registration, while `src/main/agent/runtime.ts` injects handlers backed by the main-owned parsers, native runner, service calls, app allowlist behavior, and platform failure semantics.

Web search keeps the same boundary: the harness owns the LangChain `web_search` declaration and input contract, while `src/main/agent/runtime.ts` injects the Jingle web search service handler. Provider configuration, Tavily calls, URL safety, result normalization, logging, and failure behavior stay in `src/main/services/web-tools`.

`agent:invoke` and `agent:editLastUserMessageAndInvoke` keep composer refs as the product-level `message.refs` field across renderer, preload, IPC schema, and main service. LangChain `HumanMessage.additional_kwargs.refs` is only created by `buildJingleSubmittedMessages` at the harness boundary.

`agent:resume` remains a product HITL decision contract at renderer/preload/IPC/main-service boundaries. The LangGraph `Command({ resume, update })` envelope is only created by `buildJingleResumeCommand` at the harness runtime boundary.

Next candidates should move one runtime-owned behavior at a time from `src/main/agent/*`, with the old path deleted in the same reviewable batch.
